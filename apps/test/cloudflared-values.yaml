
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: cloudflared
  namespace: network
spec:
  values:
    controllers:
      cloudflared:
        containers:
          app:
            args:
              - tunnel
              - --config
              - /etc/cloudflared/config/config.yaml
              - run
              - 51921033-9888-483b-aa37-1572275acd11
